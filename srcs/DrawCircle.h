using 
class DrawCircle {
public:
	DrawCircle(int top_x, int top_y, int size_x, int size_y, int number):
	  top_x(top_x), top_y(top_y), size_x(size_x), size_y(size_y), number(number);
private:
	int top_x, top_y, size_x, size_y;
	int number;
};
